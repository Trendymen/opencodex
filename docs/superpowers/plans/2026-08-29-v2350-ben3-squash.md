# OpenCodex v2.35.0-ben.3 全量压缩实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox () syntax for tracking.

**Goal:** 将 v2.35.0..ben.2 的 39 commits 压缩为 5 commits 并发布 v2.35.0-ben.3。

**Architecture:** 使用 git commit-tree + git read-tree 从官方基线重建 5 个提交。C3 包含 ben.2 的完整 tree，以及本轮新增的 spec/plan 两份受跟踪设计文档；其余 112 个共享路径与 ben.2 完全一致。ben.3 版本推进后走既有 candidate CI, review, tag, five-member atomic push, final CI, Release 流程。

**Tech Stack:** Bun 1.4, Git refs/tags, GitHub Actions, GitHub CLI.

**Spec:** docs/superpowers/specs/2026-08-29-v2350-ben3-squash-design.md

## Global Constraints

- 官方基线固定为 v2.35.0 / fc4de772b58c13f7b16b5029b1e981d612a5db06
- ben.2 tag v2.35.0-ben.2 peeled 42282c405dc4c3dcb4f1e2877b89ac6ab49eeaba 不可移动或删除
- C3 相对 v2.35.0 包含 112 个 ben.2 路径与 2 份本轮设计文档，共 114 个路径；112 个共享路径内容精确等于 8499fcec058d61a42a4fa382118e4c7f92bbff58
- 最终恰好 5 个 commit
- 不发布 npm，不修改全局安装服务或配置
- 工作区直接使用当前 sync/v2.35.0；不创建 worktree
- 官方 v2.35.0 Tag 保留在 origin（exact identity）
- Fork ben.3 Tag 为 annotated，不 force 不移动

## Task 1: 构建压缩历史

**Files:**
- Create: 本地分支 ben3-squash-backup
- Rebuild: sync/v2.35.0 (5 commits from v2.35.0)

**Interfaces:**
- Consumes: 当前 ben.2 tree 8499fcec058d61a42a4fa382118e4c7f92bbff58, 官方基线 fc4de772b58c13f7b16b5029b1e981d612a5db06
- Produces: 5 个新 commits C1..C5，C3 含 ben.2 tree 与本轮两份设计文档，HEAD 等于 C5

### Steps

1. git branch ben3-squash-backup 42282c405dc4c3dcb4f1e2877b89ac6ab49eeaba
2. git checkout -b ben3-work v2.35.0
3. Stage C1 paths (src/ bin/ gui/ docs-site/) from ben3-squash-backup. Delete files that were deleted. Commit as C1 (feat: 汇总 Fork 运行时与兼容扩展).
4. Stage C2 paths (tests/) from ben3-squash-backup. Commit as C2 (test: 汇总 Fork 回归与兼容覆盖).
5. Stage all remaining paths from ben3-squash-backup，并加入本轮两份 docs/superpowers spec/plan 文档。提交为 C3（chore: 汇总 Fork CI 发布与审计基础设施）。硬断言：C3 相对 v2.35.0 为 114 个路径；其中 112 个与 ben.2 共享的路径逐 blob 相同，另外仅为两份指定设计文档，否则中止。
6. C4 boundary: only modify package.json (2.35.0-ben.2 to 2.35.0-ben.3) and tests/fork-maintenance-truth.test.ts (version assertion only: change expected version string to 2.35.0-ben.3). Do NOT add ben3-squash machine block in C4. Commit as C4 (chore: 推进 v2.35.0-ben.3 版本真源).
7. C5 boundary: modify FORK_CHANGES.md (add ben.3 squash section with 39→5 count, C3 tree identity, 112-path delta, ben.2 immutable history, pending external gates). Also add ben3-squash machine block to FORK_CHANGES.md and update tests/fork-maintenance-truth.test.ts with all FORK_CHANGES-dependent assertions (including ben3-squash block parsing). Commit as C5 (docs: 记录 v2.35.0-ben.3 全量压缩与发布边界).
   - C4 acceptance: bun test tests/fork-maintenance-truth.test.ts passes at C4 (version assertion green, no ben3-squash block assertion yet).
   - C5 acceptance: bun test tests/fork-maintenance-truth.test.ts passes at C5 (all assertions green including ben3-squash block).
8. Verify git rev-list --count v2.35.0..C5 = 5.
9. Verify git diff --shortstat v2.35.0 C3 与 ben.2 一致，并额外包含两份本轮设计文档。
10. Verify git diff --name-only C3 C5 = [FORK_CHANGES.md, package.json, tests/fork-maintenance-truth.test.ts].
11. git checkout sync/v2.35.0 && git reset --hard C5 && git branch -D ben3-work

## Task 2: 本地验证

1. bun test tests/fork-maintenance-truth.test.ts
2. bun run typecheck
3. bun run privacy:scan
4. git diff --check v2.35.0...HEAD

## Task 3: Candidate 推送与 CI

1. Push C5 (which is now sync/v2.35.0 HEAD after Task 1 Step 11) using the remote ben.2 SHA as expected-SHA lease:
   git push origin --force-with-lease=refs/heads/sync/v2.35.0:42282c405dc4c3dcb4f1e2877b89ac6ab49eeaba refs/heads/sync/v2.35.0:refs/heads/sync/v2.35.0
2. Create mode-restricted controller (new, not reusing ben.2). snapshot candidate, intent candidate, dispatch ci.yml, bind candidate.
3. gh run watch CANDIDATE_RUN_ID --exit-status. Verify with controller allowlist from current ci.yml.

## Task 4: 发布前审查

SPEC_COMPLIANCE 与 CODE_QUALITY 并行 re-review. 携带 fix diff, candidate CI 证据. 无 Critical or Important 才进入 promotion.

## Task 5: Tag 与 Atomic Promotion

1. Create annotated v2.35.0-ben.3 pointing to C5. Three -m messages: title, squash history + tree identity, replacement candidate run + pending gates.
2. snapshot final + intent final using new controller.
3. Five-member atomic push:
   - main lease 42282c405dc4c3dcb4f1e2877b89ac6ab49eeaba
   - sync lease CANDIDATE (C5)
   - marker lease fc4de772b58c13f7b16b5029b1e981d612a5db06
   - refs/tags/v2.35.0 (no force no lease)
   - refs/tags/v2.35.0-ben.3 (no force no lease)
4. Local CAS: update-ref --stdin transaction to set main from 42282c405dc4c3dcb4f1e2877b89ac6ab49eeaba to CANDIDATE. upstream-release is a confirming no-op (already fc4de772b58c13f7b16b5029b1e981d612a5db06 locally and remotely). Then fetch branch tracking refs with --no-tags.

## Task 6: Final CI 与 Release

1. bind final (push/main/exact SHA). Watch and verify.
2. Create Release with apply_patch notes file, chmod 600. gh release create --verify-tag.
3. Verify metadata: tagName=name, non-draft, non-prerelease, assets empty.
4. Terminal verification: refs/tags/release consistency. Delete backup branch, controller, state, notes.
